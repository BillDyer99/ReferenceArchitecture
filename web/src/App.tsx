import { NotesList } from '@/features/notes'

function App() {
    return (
        <div>
            <h1 style={{ textAlign: 'center', marginTop: '1rem' }}>
                Reference Architecture
            </h1>
            <NotesList />
        </div>
    )
}

export default App